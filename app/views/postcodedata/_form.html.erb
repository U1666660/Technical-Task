<%= form_for(postcodedatum) do |f| %>
  <% if postcodedatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postcodedatum.errors.count, "error") %> prohibited this postcodedatum from being saved:</h2>

      <ul>
      <% postcodedatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :row_id %>
    <%= f.number_field :row_id %>
  </div>

  <div class="field">
    <%= f.label :postcode %>
    <%= f.text_field :postcode %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
